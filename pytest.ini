[pytest]
testpaths = website/tests labs/001-know-your-agent/tests
markers =
    slow: exercises a non-editable package install (may download/build)
    integration: optional live checks against FROM_MY_DESK_BASE_URL
